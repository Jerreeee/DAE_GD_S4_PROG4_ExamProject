Moving from ResourceManager to solely uses shared_ptr<Resource_t> and the entire engine passes around shared_ptr<Resource_t> to the engine only passing around ResourceHand<Resource_t> that internally stores a GUID, the ResourceManager has GetResource(GUID id) Methods to retreive the actual resource based on the GUID.

Problems:
#1
Description:
	ResourceManager internally stores resources as a vector of shared_ptr's. The GetResource(GUID id) methods return a shared_ptr to the resource. But, the entire engine passes around ResourceHandle's because it's fast and only calls GetResource(GUID id) if it needs the actual resource. So if I have a game and the Scene only stores ResourceHandles() and then Renderer::RenderTexture(textureHandle) is called on all handles then all textures will still need to be loaded because there is no shared_ptr<Texture> keeping the texture alive in the ResourceManager.
Solution:
	So in these cases, you need to cache the shared_ptr<Resource_t> if you know you will need it often. This makes sure that the resource stays alive in the ResourceManager(). So GetResource(GUID id) is just a quick retrieval of the already loaded resource instead of again loading the resource.

#2
Description:
	If the user would now manually create a Texture2D() then it would be useless because the entire engine expects ResourceHandle<Texture2D>.
Solution:
	1) Maybe we could still manually create a Texture2D and then call "ResourceHandle<Texture2D> ResourceManager::AddTexture(Texture2D&& texture)". And pass an rvalue
	to say that the ResourceManager must now manage it. And it would also return a proper handle that we can then use to pass around. But this is a bit cumbersome.
	2) Have the ResourceManager itself create ALL resources. This means that it is used as a factory. It will need a copy of the all resource constructors so it can then itself internally call the resource constructor. And immediatly add the resource to its internal tracking system + return a valid ResourceHandle to the caller.

#3
Description:
	Previously all .cpp files that worked with resources only had to include "Texture2D.h" or "Font.h" but now every file that makes use of a resource must include ResourceManager.h which is a big include and every time ResourceManager changes a lot of other dependant cpp files will also need to be recompiled
Solution:
	pimpl away ResourceManager to cpp file
	
#4
Description:
	Each time a new resource is added we need new Load(), Get() methods + a new map to store the new resources. This is annoying!
Goal:
	Have a generic base resource/asset type/class so we can store all of them in a single map + only a single GetResource() method
Solution:
	Asset "base" class that resources like Texture2D, Font, etc inherit from. So now we can have a single map of GUID to Asset's. We do still need type info though. So each resource class inheriting from Asset like Texture2D needs to implement a GetType() method so each Asset in the end can be cast to the correct type.
	
#5
Description:
	IResourceManager can only define "virtual Ref<Asset> GetAsset(Assethandle handle) = 0" which is not templated so we still need a way to cast it to the correct type
Solution:
	On the ResourceManager have a static templated function "Ref<T> GetAsset(AssetHandle handle)" and this one calls ServiceLocator::GetResourceManager().GetAsset(handle). This returns a base Asset that we can then be statically casted to the correct type T.
	
#6
Description:

TOOD: fix other things only pass handles and resolve later
or when Get() method is called force load the asset

- generic Load method
- AssetHandle/UUID by default creating a new valid UUID but by default should be invalid
- some Asset's need extra info asside from the filepath when creating an instance. => importsettings
- LoadAsset insert {path, assetHandle} pair to detect duplication but for some assetType's like Font this is not just dependant on the path but also extra metadata like the fontSize. So this extra metadata must be appended to the path to be able to make a clear distinction. Have the Import method return a struct that returns the Ref<Asset> but also a path.
- entire engine uses AssetHandle's so if you manually create a Sprite and want to render it then you need to first add it to the ResourceManager using AddAsset then you get a handle back and can use it.
- what if GetAsset(handle, async) is called and immedialty after Get(Asset, Immediate) is called. The other thread may already be loading the asset but we need it immediatly? What to do?
- sometimes a constructor needs a Ref<Asset> where the asset is already loaded otherwise it cant do anything( ex.e: Texture2D that needs a font to succesfully create a texture). But sometimes we dont need an already loaded asset to create a valid asset (e.x: sprite needs a texture but the loaded texture is not needed to succesfully construct the sprite, the texture is only needed if we call GetTexture() or GetSrcRegion() -> slution work with Ref<Asset> and SoftRef<Asset>
